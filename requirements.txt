# Weatherware — dependencies
pygame                      # window, rendering, sound/music playback
screen-brightness-control   # the ONLY real hardware effect we touch
keyboard                    # global hotkeys (Ctrl+Shift+Q kill switch)
psutil                      # read-only system stats (feeds the FAKE heat gauge)
pystray                     # system tray icon
Pillow                      # tray icon imaging + asset helpers
plyer                       # desktop notifications ("weather alerts")
pyinstaller                 # packaging for the demo build
